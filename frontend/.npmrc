# pnpm 11 enables a 24-hour minimumReleaseAge supply-chain policy by default.
# Since all installs use --frozen-lockfile (packages are pre-vetted via PR review),
# this policy adds friction without meaningful additional protection.
minimum-release-age=0
