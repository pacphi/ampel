# ACTIVE pipeline — promoted 2026-07-01 from .autopilot/queued/, retiring the prior (shipped)
# playbooks-guidance plan, which survives in git history and as the .autopilot/runs/
# playbooks-guidance.jsonl ledger. profile.yml is stack-level and shared — reused as-is.

feature_id: "fleet-overview-filters"
goal: "Make the Remediation Fleet Overview default to actionable repos: two independent, combinable toggle filters — 'only repos with open PRs' (openPrCount > 0) and 'only eligible repos' (eligible === true), both ON by default — each switchable to show all, with a distinct empty state when the filters hide every row."
spec: |
  Today FleetOverview.tsx renders `const fleet = rows ?? []` straight into the table, so every repo
  shows — including repos with zero open PRs and repos that are not remediation-eligible. Operators
  want the table to default to the rows they can act on, while still being able to widen the view.

  Two independent, combinable client-side toggle filters, both DEFAULT ON:
    1. "Only repos with open PRs" — keeps rows where `openPrCount > 0`.
    2. "Only eligible repos"      — keeps rows where `eligible === true`.
  Toggling either OFF widens the view; any combination is allowed (active filters AND together). When
  the fleet is non-empty but the active filters hide EVERY row, show a distinct "no repos match the
  current filters" empty state with an affordance to clear/relax the filters — separate from the
  existing "no repositories at all" (`remediation:fleet.empty`) state.

  Ground truth (from exploration):
    - FleetRow (frontend/src/types/remediation.ts): { repositoryId, name, openPrCount: number,
      eligible: boolean, policyState, airGapped }. The list from useFleetRemediation() ALREADY carries
      openPrCount and eligible, so this is PURELY client-side filtering — NO backend/API change.
    - FleetOverview.tsx (frontend/src/components/remediation/FleetOverview.tsx) keeps its existing
      loading / error / empty branches; add two boolean filter states (both default true), a derived
      filtered list, toggle controls rendered above the table (shadcn/ui — mirror existing component
      usage; a Switch or a toggled Button), and the new filtered-empty branch.
    - Tests: FleetOverview.test.tsx already exists (vitest; mocks useFleetRemediation via mockUseFleet;
      passthrough i18n where t() returns the key).
    - i18n: fleet strings live under `remediation:fleet` in public/locales/*/remediation.json (existing:
      title, description, tableLabel, error, empty, eligible, notEligible, airGappedOn, airGappedOff,
      preview, columns). New keys: the two toggle labels + the filtered-empty message (+ a clear/relax
      action label).

  i18n MUST be done via the ampel-i18n-builder `translate` util (real translations, not English
  placeholders): source crates/ampel-i18n-builder/.env, then a ZSH ARRAY loop
  `LANGS=(ar cs …); for L in $LANGS` (NEVER `for L in $string` — zsh will not word-split it and creates
  a bogus locale dir that generate-types then enumerates into types.ts). Regenerate
  frontend/src/i18n/types.ts (CI "Verify types are up-to-date" hard-fails on any drift) and, after
  translating, audit the two known translator pitfalls — corrupted {{interpolation}} vars and
  mistranslated brand/proper nouns — and repair.

references:
  prd:     "docs/planning/PRODUCT_SPEC.md"
  plan:    ""
  adr_dir: "docs/architecture/adr"
  ddd_dir: "docs/architecture/ddd"
  extra:
    - "frontend/src/components/remediation/FleetOverview.tsx"
    - "frontend/src/components/remediation/FleetOverview.test.tsx"
    - "frontend/src/types/remediation.ts"

trunk: main
base:  develop
autonomy: pr_ci

fix_budget: 5

# Presentation-layer only: client-side filtering over already-fetched data, no
# merge/auth/exec/data-model blast radius.
risk_phases: []

# NOTE (2026-07-01): originally a 2-phase plan (phase 0 = filters + EN i18n; phase 1 =
# translate all 27 locales). Ampel CI's i18n `missing` gate fails any PR with en-only keys,
# so phase 0's PR (#75) had to carry the full 27-locale translation to go green. That
# absorbed phase 1 entirely, so the plan was collapsed to this single phase to match what
# actually shipped. The original 2-phase plan survives as ledger record 0.
phases:
  - id: 0
    goal: "Default-actionable Fleet Overview: two combinable toggle filters (both ON by default) + a distinct filtered-empty state, with vitest specs and full 27-locale i18n."
    deliverables:
      - "FleetOverview.tsx: two boolean filter states (onlyWithPrs, onlyEligible), both default true; a derived filtered list applying the ACTIVE filters (AND) over the fleet rows (openPrCount > 0; eligible === true)."
      - "Two toggle controls rendered above the table (shadcn/ui, mirroring existing component usage), each labelled via i18n; toggling either widens the view."
      - "A distinct filtered-empty branch: when the fleet is non-empty but the filtered list is empty, render a 'no repos match the current filters' message with a control to clear/relax the filters (restore both defaults or show all). The existing zero-repos `remediation:fleet.empty` branch is unchanged."
      - "New i18n keys under remediation:fleet.filters (toggle labels, filtered-empty message, clear action, group label), translated into ALL 27 locales via the ampel-i18n-builder translate util (shell ARRAY loop; .env sourced), with frontend/src/i18n/types.ts regenerated via generate-types and a post-translate audit repairing any mistranslated domain/brand nouns."
    definition_of_done:
      - "cmd: make test-frontend passes, incl. new FleetOverview.test.tsx specs: default view hides a 0-PR repo AND an ineligible repo; toggling onlyWithPrs OFF reveals the 0-PR repo; toggling onlyEligible OFF reveals the ineligible repo; with both filters active and all rows excluded, the filtered-empty state renders; clearing filters restores rows."
      - "cmd: make lint-frontend passes; make format-check passes; pnpm --dir frontend type-check passes."
      - "cmd: ampel-i18n-builder `missing` reports no missing keys and `coverage --min-coverage 95` passes across all 27 locales; frontend/src/i18n/types.ts regenerated in sync (CI i18n-validation would pass)."
      - "grep: onlyWithPrs in frontend/src/components/remediation/FleetOverview.tsx"
      - "grep: fleet.filters in frontend/public/locales/en/remediation.json"
      - "grep:absent: TODO|FIXME in frontend/src/components/remediation/FleetOverview.tsx"
      - "prose: no backend/API change — the filtering is purely client-side over the existing useFleetRemediation() rows; the loading/error/zero-repos branches are unchanged. No raw i18n keys render; interpolation/brand-noun audit across locales is clean; no spaced locale directory exists."
    conventions: "React 19 + TS + Vite + TanStack Query + shadcn/ui. Mirror the just-shipped playbooks-guidance frontend work (component/test/i18n style). Every user-facing string via i18n. Translate new keys into all 27 locales in the SAME PR (CI's `missing` gate fails en-only keys); source crates/ampel-i18n-builder/.env first; shell ARRAY loop, never `for L in $string`; run generate-types (CI hard-fails on types.ts drift). `make lint` does NOT run formatters — run `make format-check` (prettier) locally before pushing."
    depends_on: []
    adrs:  []
    ddd:   ["Fleet", "Repository"]
