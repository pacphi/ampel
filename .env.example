# Database
DATABASE_URL=postgres://ampel:ampel@localhost:5432/ampel

# Redis (for future use)
REDIS_URL=redis://localhost:6379

# JWT Configuration
JWT_SECRET=your-secret-key-min-32-characters-long
JWT_ACCESS_EXPIRY_MINUTES=15
JWT_REFRESH_EXPIRY_DAYS=7

# Encryption (for provider PATs) - generate with: openssl rand -base64 32
ENCRYPTION_KEY=your-32-byte-base64-encryption-key==

# =============================================================================
# USER AUTHENTICATION (OAuth for logging INTO Ampel)
# =============================================================================

# GitHub OAuth (for user login to Ampel)
# Create an OAuth App at: https://github.com/settings/developers
GITHUB_AUTH_CLIENT_ID=your-github-oauth-app-client-id
GITHUB_AUTH_CLIENT_SECRET=your-github-oauth-app-client-secret
GITHUB_AUTH_REDIRECT_URI=http://localhost:8080/api/auth/github/callback

# Google OAuth (for user login to Ampel)
# Create credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8080/api/auth/google/callback

# =============================================================================
# GIT PROVIDER CONNECTIONS (PAT-based, configured by users in-app)
# =============================================================================
# NOTE: Git provider connections now use Personal Access Tokens (PATs)
# configured by each user in the app settings. No OAuth credentials needed here.
#
# Users can connect multiple accounts per provider using PATs:
# - GitHub: https://github.com/settings/tokens
# - GitLab: https://gitlab.com/-/profile/personal_access_tokens
# - Bitbucket: https://bitbucket.org/account/settings/app-passwords/
#
# Self-hosted instances (GitHub Enterprise, GitLab CE/EE, Bitbucket Server)
# are configured per-connection by the user with a custom base URL.

# =============================================================================
# LEGACY (deprecated - kept for migration compatibility)
# =============================================================================
# These were used for OAuth-based provider connections.
# Now replaced by per-user PAT-based connections.
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# GITLAB_CLIENT_ID=
# GITLAB_CLIENT_SECRET=
# BITBUCKET_CLIENT_ID=
# BITBUCKET_CLIENT_SECRET=

# Server
HOST=0.0.0.0
PORT=8080
RUST_LOG=info,ampel=debug

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Frontend
VITE_API_URL=http://localhost:8080/api
